<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

      <Spinner
        android:id="@+id/spinner_FilterSelections"
        android:text="@string/spinner_Heading"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:entries = "@array/filter_defects" />
    <Button
        android:id="@+id/button_ReportIssue"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:text="@string/report_issue"
        android:layout_alignParentBottom="true"
        />

    <!-- REMOVE the line "android:entries:" when attaching a ListAdapter onto
    to the listview to show up ACTUAL DATA -->
    <ListView
        android:id="@+id/listView_DefectsResult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/button_ReportIssue"
        android:layout_below="@id/spinner_FilterSelections"
        android:layout_marginTop="15dp"
        android:entries="@array/hardcoded_defects_records"
        android:layout_marginBottom="15dp"/>

</RelativeLayout>
